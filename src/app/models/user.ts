export interface IUserInfo {
  id: number;
  firstName: string;
  lastName: string;
  age: number;
  about: string;
  city: string;
}

export interface IUser {
  id: number;
  firstName: string;
  lastName: string;
}
